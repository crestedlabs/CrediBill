import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Subscriptions — CrediBill",
  description:
    "Manage subscription lifecycles, trials, renewals, and payment status for customers across apps.",
  keywords: ["CrediBill", "subscriptions", "billing", "lifecycle"],
};

export default function Page() {
  return <></>;
}
